# 0.2.0 验证记录

日期：2026-09-11。仅本地交付，未推送或发布私有资料。

## 构建与核心测试

- 核心 JVM：56 项通过，0 失败。
- JaCoCo 核心行覆盖率：510 / 524 = 97.33%，超过项目 80% 门槛。
- Android Debug APK 与测试 APK 构建通过。
- Android Lint：No issues found。
- 三种语言各 285 项本地资源，数量一致；动态文案使用占位模板。
- `git diff --check` 通过；私有资料、内置资源目录和 dist APK 保持忽略。

## Android 验证

使用项目 Android 35 x86_64 官方模拟器 emulator-5580，未清空已有测试数据。

- 完整回归 15 项通过，运行 77.657 秒，原始日志：`.tools/device-tests-latest.log`。
- 最后调整图示缩放原点后，追加图示缩放、共同终点、点击节点跳转测试 1 项通过，运行 9.902 秒。合计 16 项 Android 用例通过；该图示测试使用合成资料。
- 图示截图：`.tools/story-map-verification.png`，已人工式视觉检查连线、收束节点和文本是否可见。
- 独立进程重启检查：482 条记录完全保持一致，测试密钥未以明文出现在偏好存储。

新增用例覆盖：

1. 重生成不增加房间；旧版本未来消息、摘要、阶段和事实恢复。
2. 失败候选保留，原活动版本不被替换。
3. 全局语言切换，主题修改保留语言；不填写背景即可初始化故事。
4. 自动结局收束不合并历史，撤销后自动匹配不重新归入被拒绝的组。
5. 六个故事入口的生成和缓存、世界导览、翻译缓存、API 分支命名。
6. 图示缩放与路线跳转。

原有回归保留：API 流式切换及密钥隔离、退后台中断、Activity 重建、记忆表编辑、备份排除密钥、内置资源幂等、大记录与头像读取、主导航。

## TDD 与修复记录

- 首先新增 StoryTest，运行得到 ReplyText / StoryVersions 缺失导致的预期编译失败，再实现。
- 增加 Markdown 标签变体用例，确认失败后扩展前缀处理；最终全部转绿。
- Android 测试捕获 JVM 未暴露的 ICU 正则右花括号转义问题，修复并重跑通过。
- 多语言使旧测试对固定中文的隐式依赖失效，测试改为明确选择中文，主题测试保持其他设置；完整回归转绿。
- Lint 捕获组合期间直接读取 StateFlow.value，改为订阅状态。关闭语言拆包以保留运行时切换资源。
- 当前环境 .git 只读，因此未创建技能建议的检查点提交；RED/GREEN 证据记录在本次工具输出及本文件中。

## 安装包

- `dist/AI-Tavern-0.2.0-private-debug.apk`
- applicationId：`cn.aitavern.app`；versionCode：4。
- 包名与签名已和 0.1.2 比对一致，版本号递增，支持覆盖安装。
- SHA-256：`923776742F7D5B96F53FF3A1A8FBCE910AA860E32C9310B68AB8530981BA8A3E`。

## 验证边界

API 用例使用 MockWebServer，未调用真实厂商服务；未做实体手机验证。AI 对具体世界的摘要质量、翻译质量与结局判断准确率仍依赖所选模型，不能由模拟 API 测试证明。网络失败保持原始资料与已有内容，结局收束支持用户撤销。

## 自评（agent-self-evaluation）

整体 4.0 / 5。

| 维度 | 分数 | 证据与改进方向 |
| --- | --- | --- |
| 准确性 | 4 | 构建、56 项核心与16项 Android 用例支持功能结论；真实厂商输出质量尚未测量。 |
| 完整性 | 4 | 四项功能、版本迁移、备份、失败恢复及私有 APK 均已交付；实体设备验证仍待实际设备。 |
| 清晰性 | 4 | README 给出入口，本文区分模拟和真实验证；动态标签映射较集中，未来可分页面整理。 |
| 可操作性 | 4 | 安装包签名匹配可覆盖安装，保留原资料；用户仍需自备有效 API 配置。 |
| 简洁性 | 4 | 用户交付说明保持简短，详细证据集中在此；多语言资源内容较多。 |

无低于3分的关键缺口。后续优先对真实模型的六入口结构输出和结局误判率做样本评估，并在实体手机检查长列表与大分支图性能。自检：用户可以直接安装体验，但不应将模拟器与模拟服务结果视为实体手机或真实厂商实测。
